"""Framework integrations for automatic trace capture.

Currently available:

- ``agent_replay.integrations.langchain``: LangChain callback handler.
  Requires the optional ``langchain-core`` package.
- ``agent_replay.integrations.llamaindex``: LlamaIndex callback handler.
  Requires the optional ``llama-index-core`` package.
"""
